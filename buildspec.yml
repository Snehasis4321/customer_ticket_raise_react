version: 0.2
env:
  variables:
    VITE_API_URL: "http://52.15.211.59:9000"
phases:
  install:
    runtime-versions:
      nodejs: 18
    commands:
      - npm install -g pnpm
      - pnpm install
  build:
    commands:
      - pnpm run build
artifacts:
  files:
    - "dist/**/*"
    - "scripts/**"
    - "appspec.yml"
